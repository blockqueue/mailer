{
  "name": "mailer",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "remove:apps": "(cd apps/web && rm -rf node_modules .next .turbo) && (cd apps/docs && rm -rf node_modules .next .turbo) && (cd apps/mailer && rm -rf node_modules .turbo dist)",
    "remove:packages": "(cd packages/eslint-config && rm -rf node_modules) && (cd packages/ui && rm -rf node_modules .turbo)",
    "reinstall": "npm run remove:packages && npm run remove:apps && rm -rf node_modules && npm i",
    "build:packages": "(cd packages/core && npm run build)"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "prettier-plugin-organize-imports": "^4.3.0",
    "turbo": "^2.7.0",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}

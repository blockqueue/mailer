{
  "name": "mailer",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "remove:apps": "(cd apps/web && rm -rf node_modules .next .turbo) && (cd apps/docs && rm -rf node_modules .next .turbo) && (cd apps/notifier && rm -rf node_modules .turbo dist)",
    "remove:examples": "(cd examples/notifier-service && rm -rf node_modules)",
    "remove:packages": "(cd packages/eslint-config && rm -rf node_modules) && (cd packages/ui && rm -rf node_modules .turbo)",
    "reinstall": "npm run remove:packages && npm run remove:apps && npm run remove:examples && rm -rf node_modules && npm i"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "prettier-plugin-organize-imports": "^4.3.0",
    "turbo": "^2.7.0",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*",
    "examples/*"
  ]
}
